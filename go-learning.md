## Go Work - For multiple Projects in the same directory

`go work init`
`go work use 02-go-mux-crud-api`

## Go Mod

first
`go mod init "name"`

then only

`go insatll <package_name>`
`go get <package_name>`
